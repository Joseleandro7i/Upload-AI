{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": ["jest", "node"], // Add 'jest' to recognize Jest types
    "module": "ESNext", // Ensure this is set to 'ESNext' or 'ESModule'
    "target": "es2020" // Adjust according to your needs
  },
  "files": ["src/test-setup.ts"],
  "include": ["src/**/*.spec.ts", "src/**/*.d.ts"]
}

